/**
 * MorphoNews Feature: エージェンティック・行動シグナル・パルス
 * Generated: 2026-02-22_0958
 * Description: ユーザーのスクロール速度、マウスの滞在、テキスト選択などの微細な行動シグナルを自律的に解析し、関心の高いセクションに視覚的な「共鳴パルス」を生成して、コンテンツへの没入と探索を自律的に支援します。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .agentic-pulse-container { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 9999; } .signal-pulse { position: absolute; border-radius: 50%; background: rgba(0, 255, 210, 0.15); transform: scale(0); animation: signal-expand 2s cubic-bezier(0.165, 0.84, 0.44, 1) forwards; } @keyframes signal-expand { 0% { transform: scale(0); opacity: 0.8; border: 2px solid rgba(0, 255, 210, 0.5); } 100% { transform: scale(4); opacity: 0; border: 1px solid rgba(0, 255, 210, 0); } } .interest-hud { position: fixed; bottom: 20px; left: 20px; padding: 10px 15px; background: rgba(10, 15, 20, 0.85); color: #00ffd2; border-left: 3px solid #00ffd2; border-radius: 0 4px 4px 0; font-family: 'Inter', sans-serif; font-size: 11px; letter-spacing: 0.05em; pointer-events: none; opacity: 0; transition: opacity 0.3s; z-index: 10000; box-shadow: 0 4px 15px rgba(0,0,0,0.5); backdrop-filter: blur(5px); } .interest-hud.active { opacity: 1; } .interest-hud .pulse-dot { display: inline-block; width: 8px; height: 8px; background: #00ffd2; border-radius: 50%; margin-right: 8px; animation: blink 1s infinite; } @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } } `; document.head.appendChild(style); const pulseContainer = document.createElement('div'); pulseContainer.className = 'agentic-pulse-container'; document.body.appendChild(pulseContainer); const hud = document.createElement('div'); hud.className = 'interest-hud'; hud.innerHTML = '<span class="pulse-dot"></span>SIGNAL: ANALYZING INTENT'; document.body.appendChild(hud); let lastX = 0, lastY = 0; let dwellTimeout = null; let scrollActivity = 0; let activeSignals = 0; const createPulse = (x, y, size = 60) => { activeSignals++; hud.classList.add('active'); const pulse = document.createElement('div'); pulse.className = 'signal-pulse'; pulse.style.left = `${x - size/2}px`; pulse.style.top = `${y - size/2}px`; pulse.style.width = `${size}px`; pulse.style.height = `${size}px`; pulseContainer.appendChild(pulse); setTimeout(() => { pulse.remove(); activeSignals--; if(activeSignals === 0) hud.classList.remove('active'); }, 2000); }; document.addEventListener('mousemove', (e) => { lastX = e.clientX; lastY = e.clientY; clearTimeout(dwellTimeout); dwellTimeout = setTimeout(() => { hud.innerHTML = '<span class="pulse-dot"></span>SIGNAL: FOCUS ANCHORED'; createPulse(lastX, lastY, 120); }, 1500); }); window.addEventListener('scroll', () => { scrollActivity++; if(scrollActivity > 10) { hud.classList.add('active'); hud.innerHTML = '<span class="pulse-dot"></span>SIGNAL: FLOW OPTIMIZED'; scrollActivity = 0; } }); document.addEventListener('mouseup', () => { const selection = window.getSelection().toString(); if (selection.trim().length > 0) { hud.innerHTML = '<span class="pulse-dot"></span>SIGNAL: CONCEPT CAPTURED'; for(let i=0; i<3; i++) { setTimeout(() => createPulse(lastX, lastY, 180 + (i*40)), i * 300); } } }); })();
