/**
 * MorphoNews Feature: エージェンティック・フラックス・ステート・ブリッジ
 * Generated: 2026-02-10_1008
 * Description: ページ内の要素が移動・変形する際、その動的な軌跡を「エネルギーの残像」として視覚的に繋ぎ合わせます。レイアウトの変化による認知負荷を軽減し、情報の遷移を滑らかな物理的移動として脳に認識させます。
 */
(function() { const style = document.createElement('style'); style.textContent = ` #flux-bridge-canvas { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; pointer-events: none; z-index: 999999; } .flux-trace { fill: none; stroke: url(#flux-gradient); stroke-width: 3; stroke-linecap: round; filter: blur(2px); opacity: 0; animation: flux-fade 1.2s cubic-bezier(0.4, 0, 0.2, 1) forwards; } @keyframes flux-fade { 0% { opacity: 0; stroke-dasharray: 0, 1000; } 20% { opacity: 0.8; } 100% { opacity: 0; stroke-dasharray: 1000, 0; } } `; document.head.appendChild(style); const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg'); svg.id = 'flux-bridge-canvas'; const defs = document.createElementNS('http://www.w3.org/2000/svg', 'defs'); defs.innerHTML = ` <linearGradient id='flux-gradient' x1='0%' y1='0%' x2='100%'> <stop offset='0%' stop-color='#4facfe' /> <stop offset='100%' stop-color='#00f2fe' /> </linearGradient> `; svg.appendChild(defs); document.body.appendChild(svg); const elementStates = new Map(); const trackedSelectors = '.news-item, article, .topic-tag, h1, h2, .insight-card'; function getCenter(rect) { return { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 }; } function drawBridge(oldCenter, newCenter) { const path = document.createElementNS('http://www.w3.org/2000/svg', 'path'); const dx = newCenter.x - oldCenter.x; const dy = newCenter.y - oldCenter.y; const distance = Math.sqrt(dx * dx + dy * dy); if (distance < 5) return; const cp1x = oldCenter.x + dx * 0.25; const cp1y = oldCenter.y - 20; const cp2x = oldCenter.x + dx * 0.75; const cp2y = newCenter.y + 20; const d = `M ${oldCenter.x} ${oldCenter.y} C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${newCenter.x} ${newCenter.y}`; path.setAttribute('d', d); path.setAttribute('class', 'flux-trace'); svg.appendChild(path); setTimeout(() => path.remove(), 1300); } function captureStates() { document.querySelectorAll(trackedSelectors).forEach((el, i) => { const id = el.dataset.fluxId || `flux-${i}`; el.dataset.fluxId = id; const rect = el.getBoundingClientRect(); const center = getCenter(rect); if (elementStates.has(id)) { const prevState = elementStates.get(id); if (prevState.x !== center.x || prevState.y !== center.y) { drawBridge(prevState, center); } } elementStates.set(id, { x: center.x, y: center.y }); }); } const observer = new MutationObserver(() => { requestAnimationFrame(captureStates); }); observer.observe(document.body, { attributes: true, childList: true, subtree: true, characterData: true }); window.addEventListener('scroll', () => { requestAnimationFrame(captureStates); }, { passive: true }); captureStates(); })();
