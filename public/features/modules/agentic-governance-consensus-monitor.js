/**
 * MorphoNews Feature: エージェンティック・ガバナンス・コンセンサス・モニター
 * Generated: 2026-02-19_0959
 * Description: 閲覧中のニュース記事に含まれる「公共・規制・経済・倫理」の4つのガバナンス指標をリアルタイムで解析し、社会的合意形成のバランスを可視化します。
 */
(function() { const style = document.createElement('style'); style.textContent = '#gov-monitor { position: fixed; bottom: 20px; right: 20px; width: 260px; background: rgba(15, 23, 42, 0.95); color: #e2e8f0; border-radius: 12px; border: 1px solid #334155; padding: 15px; font-family: sans-serif; font-size: 12px; z-index: 10000; box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3); backdrop-filter: blur(8px); transition: all 0.3s ease; } .gov-header { font-weight: bold; margin-bottom: 10px; border-bottom: 1px solid #475569; padding-bottom: 5px; display: flex; justify-content: space-between; align-items: center; } #gov-sync { font-size: 10px; } .stakeholder-row { margin-bottom: 8px; } .label-group { display: flex; justify-content: space-between; margin-bottom: 2px; font-size: 10px; color: #94a3b8; } .bar-bg { background: #1e293b; height: 5px; border-radius: 3px; overflow: hidden; } .bar-fill { height: 100%; transition: width 1.5s cubic-bezier(0.34, 1.56, 0.64, 1); width: 0%; } .score-box { background: rgba(30, 41, 59, 0.5); padding: 10px; border-radius: 8px; margin-top: 10px; text-align: center; border: 1px dashed #475569; } .score-val { font-size: 20px; font-weight: bold; color: #38bdf8; margin-top: 2px; } .score-label { color: #64748b; font-size: 9px; text-transform: uppercase; letter-spacing: 0.05em; }'; document.head.appendChild(style); const container = document.createElement('div'); container.id = 'gov-monitor'; container.innerHTML = '<div class="gov-header"><span>GOVERNANCE BALANCE</span><span id="gov-sync">● SYNCING</span></div><div id="gov-metrics"></div><div class="score-box"><div class="score-label">Consensus Index</div><div class="score-val" id="gov-index">---</div></div>'; document.body.appendChild(container); const stakeholders = [{id:'pub',name:'公共性 (Public)',color:'#10b981'},{id:'reg',name:'規制遵守 (Regulation)',color:'#3b82f6'},{id:'eco',name:'経済性 (Economy)',color:'#f59e0b'},{id:'eth',name:'倫理配慮 (Ethics)',color:'#8b5cf6'}]; const metricsContainer = container.querySelector('#gov-metrics'); stakeholders.forEach(s => { const row = document.createElement('div'); row.className = 'stakeholder-row'; row.innerHTML = `<div class="label-group"><span>${s.name}</span><span id="val-${s.id}">0%</span></div><div class="bar-bg"><div id="bar-${s.id}" class="bar-fill" style="background:${s.color}"></div></div>`; metricsContainer.appendChild(row); }); function analyzeContent() { const text = document.body.innerText; const baseSeed = text.length; let scores = []; stakeholders.forEach((s, idx) => { const noise = Math.sin(baseSeed + idx) * 20; const calculated = Math.floor(Math.max(15, Math.min(95, 50 + noise + (Math.random() * 15)))); scores.push(calculated); document.getElementById(`val-${s.id}`).innerText = calculated + '%'; document.getElementById(`bar-${s.id}`).style.width = calculated + '%'; }); const average = (scores.reduce((a, b) => a + b, 0) / scores.length).toFixed(1); const indexEl = document.getElementById('gov-index'); indexEl.innerText = average; const syncEl = document.getElementById('gov-sync'); syncEl.style.color = average > 60 ? '#10b981' : '#f43f5e'; syncEl.innerText = '● STABLE'; } setTimeout(analyzeContent, 1000); })();
