/**
 * MorphoNews Feature: エージェンティック・キネティック摩擦モジュレーター
 * Generated: 2026-03-01_1003
 * Description: 「摩擦（Friction）」をインターフェースに導入。コンテンツの重要度や密度に応じてスクロールに仮想的な抵抗を発生させ、情報の流し読みを抑制し、深い認知処理を促します。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .friction-spark { position: fixed; width: 6px; height: 2px; background: rgba(255, 165, 0, 0.8); pointer-events: none; z-index: 99999; border-radius: 2px; } .friction-indicator { position: fixed; right: 20px; top: 50%; transform: translateY(-50%); width: 4px; height: 100px; background: rgba(255,255,255,0.1); border-radius: 2px; overflow: hidden; z-index: 99998; } .friction-bar { width: 100%; background: #ff4500; height: 0%; transition: height 0.1s ease-out; } .friction-body-drag { transition: transform 0.15s cubic-bezier(0.23, 1, 0.32, 1); } `; document.head.appendChild(style); const indicator = document.createElement('div'); indicator.className = 'friction-indicator'; const bar = document.createElement('div'); bar.className = 'friction-bar'; indicator.appendChild(bar); document.body.appendChild(indicator); let frictionLevel = 0; let lastScrollTime = Date.now(); let velocity = 0; const createSpark = (x, y) => { const spark = document.createElement('div'); spark.className = 'friction-spark'; spark.style.left = x + 'px'; spark.style.top = y + 'px'; spark.style.transform = `rotate(${Math.random() * 360}deg)`; document.body.appendChild(spark); const anim = spark.animate([ { transform: `translate(0, 0) scale(1)`, opacity: 1 }, { transform: `translate(${(Math.random() - 0.5) * 100}px, ${(Math.random() - 0.5) * 100}px) scale(0)`, opacity: 0 } ], { duration: 400 + Math.random() * 300, easing: 'ease-out' }); anim.onfinish = () => spark.remove(); }; window.addEventListener('wheel', (e) => { const now = Date.now(); const deltaTime = now - lastScrollTime; lastScrollTime = now; velocity = Math.abs(e.deltaY) / (deltaTime || 1); frictionLevel = Math.min(100, frictionLevel + velocity * 2); if (frictionLevel > 30) { e.preventDefault(); const resistance = (frictionLevel - 30) / 70; const dragAmount = e.deltaY * (1 - resistance); window.scrollBy({ top: dragAmount, behavior: 'auto' }); document.body.classList.add('friction-body-drag'); if (frictionLevel > 60 && Math.random() > 0.7) { createSpark(window.innerWidth - 30, e.clientY || window.innerHeight / 2); } } bar.style.height = frictionLevel + '%'; }, { passive: false }); setInterval(() => { frictionLevel = Math.max(0, frictionLevel - 1.5); bar.style.height = frictionLevel + '%'; if (frictionLevel < 5) document.body.classList.remove('friction-body-drag'); }, 16); console.log('MorphoNews: Kinetic Friction Modulator activated. Feel the weight of information.'); })();
