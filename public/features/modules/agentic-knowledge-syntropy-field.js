/**
 * MorphoNews Feature: エージェンティック・ナレッジ・シントロピー・フィールド
 * Generated: 2026-02-07_0953
 * Description: ページ内のテキスト情報密度を自律的に解析し、情報の秩序（シントロピー）が高まっている領域を視覚的なエネルギー磁場として描画します。重要度の高いセクション間を流動的な粒子が繋ぐことで、読者の潜在意識にコンテンツの構造を提示します。
 */
(function() { const style = document.createElement('style'); style.textContent = '#syntropy-canvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: -1; opacity: 0.5; transition: opacity 1s ease; } .syntropy-highlight { transition: box-shadow 0.5s ease; }'; document.head.appendChild(style); const canvas = document.createElement('canvas'); canvas.id = 'syntropy-canvas'; document.body.appendChild(canvas); const ctx = canvas.getContext('2d'); let particles = []; let nodes = []; function resize() { canvas.width = window.innerWidth; canvas.height = window.innerHeight; } window.addEventListener('resize', resize); resize(); function analyzeNodes() { const elements = document.querySelectorAll('p, h1, h2, h3, article'); const scrollY = window.scrollY; nodes = Array.from(elements).filter(el => el.innerText.length > 20).map(el => { const rect = el.getBoundingClientRect(); return { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2, weight: Math.min(el.innerText.length / 500, 1.5), color: `hsla(${180 + (el.innerText.length % 60)}, 70%, 60%, 0.4)` }; }); } class Particle { constructor() { this.init(); } init() { this.x = Math.random() * canvas.width; this.y = Math.random() * canvas.height; this.vx = (Math.random() - 0.5) * 2; this.vy = (Math.random() - 0.5) * 2; this.size = Math.random() * 2 + 1; this.life = Math.random() * 0.5 + 0.5; } update() { nodes.forEach(node => { const dx = node.x - this.x; const dy = node.y - this.y; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < 400) { const force = (400 - dist) / 4000; this.vx += dx * force * node.weight; this.vy += dy * force * node.weight; } }); this.x += this.vx; this.y += this.vy; this.vx *= 0.94; this.vy *= 0.94; if (this.x < 0 || this.x > canvas.width || this.y < 0 || this.y > canvas.height) { this.init(); } } draw() { ctx.fillStyle = `rgba(100, 200, 255, ${this.life})`; ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2); ctx.fill(); } } for (let i = 0; i < 120; i++) { particles.push(new Particle()); } function animate() { ctx.clearRect(0, 0, canvas.width, canvas.height); particles.forEach(p => { p.update(); p.draw(); }); nodes.forEach(node => { ctx.beginPath(); ctx.arc(node.x, node.y, node.weight * 30, 0, Math.PI * 2); ctx.fillStyle = node.color; ctx.fill(); }); requestAnimationFrame(animate); } analyzeNodes(); window.addEventListener('scroll', () => { analyzeNodes(); }); setInterval(analyzeNodes, 2000); animate(); })();
