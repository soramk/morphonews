/**
 * MorphoNews Feature: エージェンティック・ナラティブ・スカフォールド・ビルダー
 * Generated: 2026-02-04_0953
 * Description: 読者の現在の視点と次に読むべき情報の間に動的な「物語の足場（スカフォールド）」を構築します。視線やスクロールの動きから関心の遷移を予測し、文脈を繋ぐキーワードを空間上に浮遊させ、情報の断片を一つの連続した物語へと再構成します。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .morpho-scaffold-container { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 9999; overflow: hidden; } .scaffold-line { position: absolute; background: linear-gradient(90deg, transparent, rgba(0, 255, 180, 0.4), transparent); height: 2px; transform-origin: left center; transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1); filter: blur(1px); } .scaffold-node { position: absolute; padding: 4px 12px; background: rgba(0, 40, 40, 0.8); border: 1px solid rgba(0, 255, 180, 0.6); border-radius: 12px; color: #00ffb4; font-size: 10px; font-family: 'monospace'; white-space: nowrap; box-shadow: 0 0 10px rgba(0, 255, 180, 0.3); animation: scaffold-float 4s ease-in-out infinite; } @keyframes scaffold-float { 0%, 100% { transform: translateY(0); opacity: 0.7; } 50% { transform: translateY(-10px); opacity: 1; } } `; document.head.appendChild(style); const container = document.createElement('div'); container.className = 'morpho-scaffold-container'; document.body.appendChild(container); let lastActiveElement = null; const getKeywords = (text) => { const words = text.split(/\s+|[、。]/).filter(w => w.length > 2); return words.slice(0, 3); }; const buildScaffold = () => { const elements = document.querySelectorAll('h1, h2, h3, p'); let currentElement = null; const viewportCenter = window.innerHeight / 2; elements.forEach(el => { const rect = el.getBoundingClientRect(); if (rect.top < viewportCenter && rect.bottom > viewportCenter * 0.5) { currentElement = el; } }); if (currentElement && currentElement !== lastActiveElement) { lastActiveElement = currentElement; container.innerHTML = ''; const nextElement = currentElement.nextElementSibling; if (nextElement) { const rect1 = currentElement.getBoundingClientRect(); const rect2 = nextElement.getBoundingClientRect(); const line = document.createElement('div'); line.className = 'scaffold-line'; const dx = 50; const dy = rect2.top - rect1.bottom; const length = Math.sqrt(dx * dx + dy * dy); line.style.width = `${length}px`; line.style.left = `${rect1.left + 20}px`; line.style.top = `${rect1.bottom + (dy/2)}px`; line.style.transform = `rotate(${Math.atan2(dy, dx)}rad)`; container.appendChild(line); const keywords = getKeywords(currentElement.innerText + ' ' + nextElement.innerText); keywords.forEach((word, i) => { const node = document.createElement('div'); node.className = 'scaffold-node'; node.textContent = `AGENTIC_BRIDGE >> ${word}`; node.style.left = `${rect1.left + 40 + (i * 60)}px`; node.style.top = `${rect1.bottom + (dy/2) - 30}px`; container.appendChild(node); }); } } }; window.addEventListener('scroll', () => { requestAnimationFrame(buildScaffold); }); buildScaffold(); })();
