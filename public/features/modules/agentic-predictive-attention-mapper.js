/**
 * MorphoNews Feature: エージェンティック・予測的アテンション・マッパー
 * Generated: 2026-02-17_0958
 * Description: ユーザーのマウス軌跡とスクロール慣性から「次なる関心ポイント」をAIエージェントが自律的に予測。予測されたエリアを微細な視覚的共鳴（パルス）で強調し、情報のシームレスな受容をガイドします。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .agentic-prediction-node { pointer-events: none; position: fixed; width: 100px; height: 100px; border-radius: 50%; background: radial-gradient(circle, rgba(0, 255, 157, 0.15) 0%, transparent 70%); border: 1px solid rgba(0, 255, 157, 0.2); z-index: 9999; transform: translate(-50%, -50%); transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1); opacity: 0; filter: blur(4px); } .agentic-prediction-active { opacity: 1; transform: translate(-50%, -50%) scale(1.5); } .agentic-focus-target { position: relative; transition: filter 0.3s ease; } .agentic-focus-highlight { filter: brightness(1.2) contrast(1.1); text-shadow: 0 0 8px rgba(0, 255, 157, 0.3); } .agentic-hud-monitor { position: fixed; bottom: 20px; left: 20px; font-family: 'Courier New', monospace; font-size: 10px; color: #00ff9d; background: rgba(0, 0, 0, 0.7); padding: 8px; border: 1px solid #00ff9d; border-radius: 4px; z-index: 10000; pointer-events: none; } `; document.head.appendChild(style); const node = document.createElement('div'); node.className = 'agentic-prediction-node'; document.body.appendChild(node); const hud = document.createElement('div'); hud.className = 'agentic-hud-monitor'; hud.innerHTML = 'AGENTIC PREDICTION: STANDBY'; document.body.appendChild(hud); let lastPos = { x: 0, y: 0 }; let velocity = { x: 0, y: 0 }; let predictedPos = { x: 0, y: 0 }; let lastTarget = null; document.addEventListener('mousemove', (e) => { velocity.x = e.clientX - lastPos.x; velocity.y = e.clientY - lastPos.y; lastPos.x = e.clientX; lastPos.y = e.clientY; const forecastFactor = 15; predictedPos.x = e.clientX + (velocity.x * forecastFactor); predictedPos.y = e.clientY + (velocity.y * forecastFactor); node.classList.add('agentic-prediction-active'); node.style.left = predictedPos.x + 'px'; node.style.top = predictedPos.y + 'px'; const elementAtPrediction = document.elementFromPoint(predictedPos.x, predictedPos.y); if (elementAtPrediction && (elementAtPrediction.tagName === 'P' || elementAtPrediction.tagName === 'H1' || elementAtPrediction.tagName === 'H2')) { if (lastTarget) lastTarget.classList.remove('agentic-focus-highlight'); elementAtPrediction.classList.add('agentic-focus-highlight'); lastTarget = elementAtPrediction; hud.innerHTML = `PREDICTED INTENT: CONTENT_ABSORPTION<br>X: ${Math.round(predictedPos.x)} Y: ${Math.round(predictedPos.y)}`; } else { hud.innerHTML = 'PREDICTED INTENT: NAVIGATION_SCAN'; } }); document.addEventListener('mouseleave', () => { node.classList.remove('agentic-prediction-active'); if (lastTarget) lastTarget.classList.remove('agentic-focus-highlight'); }); let scrollTimeout; window.addEventListener('scroll', () => { node.classList.remove('agentic-prediction-active'); hud.innerHTML = 'INTENT: PERSPECTIVE_SHIFT'; clearTimeout(scrollTimeout); scrollTimeout = setTimeout(() => { hud.innerHTML = 'AGENTIC PREDICTION: RECALIBRATING...'; }, 200); }); })();
