/**
 * MorphoNews Feature: エージェンティック・セイスミック・ストラクチャー・フラクチャラー
 * Generated: 2026-02-24_0956
 * Description: 情報の重要度や論理的な矛盾を検知し、既存のUIレイアウトを物理的に「断層化」させて破壊します。グリッドを意図的にずらすことで、情報の境界を再認識させ、ユーザーの固定観念を揺さぶる構造破壊エンジンです。
 */
(function() { const style = document.createElement('style'); style.textContent = `.seismic-active .news-renderer, .seismic-active article, .seismic-active section { transition: transform 0.8s cubic-bezier(0.22, 1, 0.36, 1); } .seismic-fracture-line { position: fixed; background: rgba(0, 255, 255, 0.2); pointer-events: none; z-index: 10000; mix-blend-mode: overlay; } .seismic-control-panel { position: fixed; bottom: 20px; right: 20px; z-index: 10001; background: rgba(0, 0, 0, 0.8); color: #0f0; padding: 10px; border: 1px solid #0f0; font-family: monospace; font-size: 10px; border-radius: 4px; box-shadow: 0 0 15px rgba(0, 255, 0, 0.3); } .seismic-glitch { animation: seismic-glitch-anim 0.2s infinite; } @keyframes seismic-glitch-anim { 0% { transform: translate(0); } 20% { transform: translate(-2px, 2px); } 40% { transform: translate(-2px, -2px); } 60% { transform: translate(2px, 2px); } 80% { transform: translate(2px, -2px); } 100% { transform: translate(0); } }`; document.head.appendChild(style); const panel = document.createElement('div'); panel.className = 'seismic-control-panel'; panel.innerHTML = 'STRUCTURAL DISRUPTION: ACTIVE<br>SEISMIC_LEVEL: <span id="seismic-val">0</span>%'; document.body.appendChild(panel); let seismicLevel = 0; let active = false; function createFractureLine() { const line = document.createElement('div'); line.className = 'seismic-fracture-line'; const isVertical = Math.random() > 0.5; if (isVertical) { line.style.width = '1px'; line.style.height = '100%'; line.style.left = Math.random() * 100 + '%'; line.style.top = '0'; } else { line.style.height = '1px'; line.style.width = '100%'; line.style.top = Math.random() * 100 + '%'; line.style.left = '0'; } document.body.appendChild(line); setTimeout(() => line.remove(), 2000); } function fractureLayout() { if (!active) return; document.body.classList.add('seismic-active'); const targets = document.querySelectorAll('article, section, .news-renderer, h1, h2'); targets.forEach(el => { const shiftX = (Math.random() - 0.5) * seismicLevel * 0.5; const shiftY = (Math.random() - 0.5) * seismicLevel * 0.5; const rotate = (Math.random() - 0.5) * (seismicLevel / 10); el.style.transform = `translate(${shiftX}px, ${shiftY}px) rotate(${rotate}deg)`; if (seismicLevel > 50) { el.classList.add('seismic-glitch'); } else { el.classList.remove('seismic-glitch'); } }); if (Math.random() * 100 < seismicLevel) { createFractureLine(); } } window.addEventListener('scroll', () => { active = true; seismicLevel = Math.min(100, seismicLevel + 2); document.getElementById('seismic-val').innerText = Math.floor(seismicLevel); fractureLayout(); }); setInterval(() => { if (seismicLevel > 0) { seismicLevel -= 0.5; document.getElementById('seismic-val').innerText = Math.floor(seismicLevel); if (seismicLevel <= 0) { active = false; document.body.classList.remove('seismic-active'); document.querySelectorAll('article, section, .news-renderer, h1, h2').forEach(el => { el.style.transform = ''; el.classList.remove('seismic-glitch'); }); } else { fractureLayout(); } } }, 100); })();
