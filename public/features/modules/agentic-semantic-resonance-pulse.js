/**
 * MorphoNews Feature: エージェンティック・セマンティック・レゾナンス・パルス
 * Generated: 2026-02-11_1006
 * Description: 現在閲覧中のセクションとページ内の他セクションとの意味的関連性を自律的に検知し、共鳴（パルス）エフェクトによって情報のネットワークを視覚化します。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .resonate-target { transition: all 0.6s cubic-bezier(0.23, 1, 0.32, 1); position: relative; } .resonate-active { background: rgba(0, 255, 180, 0.1) !important; box-shadow: 0 0 20px rgba(0, 255, 180, 0.2); border-left: 4px solid #00ffb4; transform: translateX(10px); } .resonance-hud { position: fixed; bottom: 20px; right: 20px; background: rgba(15, 25, 35, 0.9); color: #00ffb4; padding: 12px; border: 1px solid #00ffb4; border-radius: 4px; font-family: 'Courier New', monospace; font-size: 11px; z-index: 10000; pointer-events: none; opacity: 0; transition: opacity 0.3s; box-shadow: 0 4px 15px rgba(0,0,0,0.5); } .resonance-hud.visible { opacity: 1; } .resonance-particle { position: absolute; width: 4px; height: 4px; background: #00ffb4; border-radius: 50%; pointer-events: none; z-index: 9999; } `; document.head.appendChild(style); const hud = document.createElement('div'); hud.className = 'resonance-hud'; hud.innerHTML = 'AGENTIC RESONANCE: IDLE'; document.body.appendChild(hud); const getKeywords = (text) => { return text.toLowerCase().replace(/[.,/#!$%^&*;:{}=\-_`~()]/g, '').split(/\s+/).filter(w => w.length > 4); }; const analyzeSections = () => { const sections = Array.from(document.querySelectorAll('p, h1, h2, h3, article section')); return sections.map((el, index) => ({ el, text: el.innerText, keywords: getKeywords(el.innerText), id: index })); }; const sectionsData = analyzeSections(); let activeTimeout; const triggerResonance = (sourceEl) => { const sourceText = sourceEl.innerText; const sourceKeywords = getKeywords(sourceText); if (sourceKeywords.length === 0) return; sectionsData.forEach(data => { if (data.el === sourceEl) return; const matches = data.keywords.filter(kw => sourceKeywords.includes(kw)); if (matches.length >= 1) { data.el.classList.add('resonate-active'); createParticleEffect(sourceEl, data.el); } else { data.el.classList.remove('resonate-active'); } }); hud.classList.add('visible'); hud.innerHTML = `AGENTIC RESONANCE: ACTIVE<br>SIMILARITY MAPPED: ${document.querySelectorAll('.resonate-active').length} NODES`; clearTimeout(activeTimeout); activeTimeout = setTimeout(() => { sectionsData.forEach(d => d.el.classList.remove('resonate-active')); hud.classList.remove('visible'); }, 3000); }; const createParticleEffect = (startEl, endEl) => { const startRect = startEl.getBoundingClientRect(); const endRect = endEl.getBoundingClientRect(); const particle = document.createElement('div'); particle.className = 'resonance-particle'; document.body.appendChild(particle); const startX = startRect.left + startRect.width / 2 + window.scrollX; const startY = startRect.top + startRect.height / 2 + window.scrollY; const endX = endRect.left + endRect.width / 2 + window.scrollX; const endY = endRect.top + endRect.height / 2 + window.scrollY; particle.style.left = startX + 'px'; particle.style.top = startY + 'px'; const animation = particle.animate([ { left: startX + 'px', top: startY + 'px', opacity: 1 }, { left: endX + 'px', top: endY + 'px', opacity: 0 } ], { duration: 800, easing: 'ease-out' }); animation.onfinish = () => particle.remove(); }; sectionsData.forEach(data => { data.el.classList.add('resonate-target'); data.el.addEventListener('mouseenter', () => triggerResonance(data.el)); }); })();
