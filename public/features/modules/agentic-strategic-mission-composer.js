/**
 * MorphoNews Feature: エージェンティック・戦略ミッション・コンポーザー
 * Generated: 2026-02-12_0959
 * Description: 閲覧中のニュース記事から戦略的キーワードを抽出し、ユーザーを「能動的エージェント」と見立てた動的な戦術ブリーフィングHUDを生成します。情報を「消費」するのではなく「遂行すべきミッション」として再定義します。
 */
(function() { const style = document.createElement('style'); style.textContent = ` #agentic-hud { position: fixed; top: 20px; right: 20px; width: 280px; background: rgba(0, 20, 10, 0.85); border: 1px solid #00ff88; color: #00ff88; font-family: 'Courier New', monospace; padding: 15px; border-radius: 4px; z-index: 10000; box-shadow: 0 0 15px rgba(0, 255, 136, 0.3); pointer-events: none; transition: opacity 0.5s ease; font-size: 12px; } .hud-title { font-weight: bold; border-bottom: 1px solid #00ff88; margin-bottom: 10px; padding-bottom: 5px; text-transform: uppercase; letter-spacing: 2px; } .hud-mission-line { margin-bottom: 8px; opacity: 0; transform: translateX(10px); animation: hudFadeIn 0.5s forwards; } @keyframes hudFadeIn { to { opacity: 1; transform: translateX(0); } } .hud-status { display: flex; justify-content: space-between; margin-top: 15px; font-size: 10px; border-top: 1px solid rgba(0, 255, 136, 0.3); pt: 5px; } .hud-scan-line { position: absolute; left: 0; width: 100%; height: 2px; background: rgba(0, 255, 136, 0.5); top: 0; animation: hudScan 4s linear infinite; } @keyframes hudScan { from { top: 0; } to { top: 100%; } } `; document.head.appendChild(style); const hud = document.createElement('div'); hud.id = 'agentic-hud'; hud.innerHTML = ` <div class='hud-scan-line'></div> <div class='hud-title'>Tactical Briefing</div> <div id='hud-content'></div> <div class='hud-status'> <span>STATUS: ACTIVE</span> <span id='hud-timer'>00:00:00</span> </div> `; document.body.appendChild(hud); const actionVerbs = ['Analyze', 'Neutralize', 'Synthesize', 'Intercept', 'Deploy', 'Monitor', 'Infiltrate', 'Calibrate', 'Secure', 'Execute']; function extractKeywords() { const text = Array.from(document.querySelectorAll('h1, h2, p')).map(el => el.innerText).join(' '); const commonWords = ['the', 'and', 'that', 'this', 'with', 'from']; const words = text.split(/\s+/).filter(w => w.length > 5 && !commonWords.includes(w.toLowerCase())).map(w => w.replace(/[^a-zA-Z]/g, '')); return [...new Set(words)].slice(0, 20); } function updateMission() { const keywords = extractKeywords(); const content = document.getElementById('hud-content'); content.innerHTML = ''; if (keywords.length < 3) { content.innerHTML = '<div class="hud-mission-line">WAITING FOR INTEL...</div>'; return; } for (let i = 0; i < 3; i++) { const verb = actionVerbs[Math.floor(Math.random() * actionVerbs.length)]; const target = keywords[Math.floor(Math.random() * keywords.length)]; const line = document.createElement('div'); line.className = 'hud-mission-line'; line.style.animationDelay = (i * 0.2) + 's'; line.textContent = `OBJ ${i + 1}: ${verb} ${target.toUpperCase()}`; content.appendChild(line); } } setInterval(() => { const now = new Date(); const timeStr = now.getHours().toString().padStart(2, '0') + ':' + now.getMinutes().toString().padStart(2, '0') + ':' + now.getSeconds().toString().padStart(2, '0'); document.getElementById('hud-timer').textContent = timeStr; }, 1000); let updateTimeout; window.addEventListener('scroll', () => { clearTimeout(updateTimeout); updateTimeout = setTimeout(updateMission, 500); }); updateMission(); })();
