/**
 * MorphoNews Feature: エージェンティック・ストラクチャル・マージャー・ニュークリアス
 * Generated: 2026-02-03_0959
 * Description: ページ内の分散したニュース要素を物理的に中央の一点（核）へと収束させ、情報の断片を統合的な「要約カード」として再構成する、収束（Convergence）を象徴する機能です。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .mn-nucleus-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.85); z-index: 9999; display: none; align-items: center; justify-content: center; backdrop-filter: blur(10px); } .mn-nucleus-core { position: relative; width: 300px; height: 300px; background: radial-gradient(circle, #fff, #00d4ff); border-radius: 50%; box-shadow: 0 0 50px #00d4ff, 0 0 100px #fff; display: flex; align-items: center; justify-content: center; transition: all 1s cubic-bezier(0.19, 1, 0.22, 1); cursor: pointer; } .mn-nucleus-card { position: absolute; width: 400px; background: white; padding: 20px; border-radius: 12px; color: #333; opacity: 0; transform: scale(0.8); transition: all 0.5s ease; box-shadow: 0 20px 40px rgba(0,0,0,0.5); font-family: sans-serif; pointer-events: none; } .mn-nucleus-active .mn-nucleus-card { opacity: 1; transform: scale(1); pointer-events: auto; } .mn-converging-particle { position: fixed; pointer-events: none; background: #00d4ff; height: 2px; height: 2px; border-radius: 50%; z-index: 10000; } .mn-trigger-btn { position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%); padding: 10px 20px; background: #00d4ff; color: white; border: none; border-radius: 20px; cursor: pointer; font-weight: bold; box-shadow: 0 4px 15px rgba(0,212,255,0.4); z-index: 9000; transition: all 0.3s; } .mn-trigger-btn:hover { background: #008eb3; transform: translateX(-50%) scale(1.05); } `; document.head.appendChild(style); const overlay = document.createElement('div'); overlay.className = 'mn-nucleus-overlay'; const core = document.createElement('div'); core.className = 'mn-nucleus-core'; const card = document.createElement('div'); card.className = 'mn-nucleus-card'; overlay.appendChild(core); overlay.appendChild(card); document.body.appendChild(overlay); const btn = document.createElement('button'); btn.className = 'mn-trigger-btn'; btn.innerText = 'CONVERGENCE EXECUTE'; document.body.appendChild(btn); btn.onclick = () => { overlay.style.display = 'flex'; const elements = Array.from(document.querySelectorAll('h1, h2, h3, p')).slice(0, 15); const centerX = window.innerWidth / 2; const centerY = window.innerHeight / 2; elements.forEach((el, i) => { const rect = el.getBoundingClientRect(); const particle = document.createElement('div'); particle.className = 'mn-converging-particle'; particle.style.left = rect.left + 'px'; particle.style.top = rect.top + 'px'; particle.style.width = rect.width + 'px'; particle.style.height = '2px'; document.body.appendChild(particle); setTimeout(() => { particle.style.transition = 'all 1.2s cubic-bezier(0.55, 0, 0.45, 1)'; particle.style.left = centerX + 'px'; particle.style.top = centerY + 'px'; particle.style.width = '0px'; particle.style.opacity = '0'; }, 50 * i); setTimeout(() => particle.remove(), 1500); }); setTimeout(() => { overlay.classList.add('mn-nucleus-active'); core.style.transform = 'scale(0.2)'; const headlines = Array.from(document.querySelectorAll('h1, h2')).slice(0, 3).map(h => h.innerText); card.innerHTML = `<h3>収束済みインサイト</h3><ul>${headlines.map(text => `<li>${text}</li>`).join('')}</ul><p style='font-size:0.8em; color:#666;'>全ての情報の断片はここに統合されました。</p><button id='mn-close-nucleus' style='margin-top:10px; cursor:pointer;'>閉じる</button>`; document.getElementById('mn-close-nucleus').onclick = (e) => { e.stopPropagation(); overlay.style.display = 'none'; overlay.classList.remove('mn-nucleus-active'); core.style.transform = 'scale(1)'; }; }, 1200); }; })();
