/**
 * MorphoNews Feature: エージェンティック・シンセシス・インテグレーター
 * Generated: 2026-02-15_1001
 * Description: サイト内の複数の文脈や断片化された情報を動的に抽出し、現在のユーザーに最も関連性の高い「統合された洞察（インサイト）」と「推奨アクション」を1つのビューに集約・提示します。
 */
(function() { const id = 'agentic-synthesis-integrator'; if (document.getElementById(id)) return; const style = document.createElement('style'); style.textContent = ` #${id} { position: fixed; bottom: 20px; left: 20px; width: 340px; background: rgba(10, 15, 25, 0.9); color: #f8fafc; border: 1px solid #4f46e5; border-radius: 12px; padding: 16px; font-family: 'Inter', system-ui, sans-serif; z-index: 10000; box-shadow: 0 15px 35px -5px rgba(0, 0, 0, 0.5); backdrop-filter: blur(8px); transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1); border-left: 4px solid #4f46e5; } #${id}.minimized { transform: translateX(-310px); opacity: 0.6; } #${id} .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; border-bottom: 1px solid #1e293b; padding-bottom: 8px; cursor: pointer; } #${id} .title { font-size: 12px; font-weight: 800; letter-spacing: 0.05em; color: #818cf8; text-transform: uppercase; } #${id} .insight-box { margin-bottom: 12px; } #${id} .label { font-size: 10px; background: #312e81; color: #c7d2fe; padding: 2px 6px; border-radius: 4px; margin-bottom: 6px; display: inline-block; } #${id} .content-text { font-size: 13px; line-height: 1.5; color: #cbd5e1; } #${id} .action-list { margin: 8px 0 0 0; padding-left: 18px; font-size: 12px; color: #94a3b8; } #${id} .consolidate-trigger { width: 100%; padding: 8px; background: linear-gradient(135deg, #4f46e5, #7c3aed); border: none; border-radius: 6px; color: white; font-weight: bold; font-size: 12px; cursor: pointer; transition: opacity 0.2s; margin-top: 10px; } #${id} .consolidate-trigger:hover { opacity: 0.9; } `; document.head.appendChild(style); const container = document.createElement('div'); container.id = id; container.innerHTML = ` <div class="header" id="${id}-header"> <span class="title">Consolidation Engine</span> <span id="${id}-toggle-icon">◀</span> </div> <div id="${id}-body"> <div class="insight-box"> <span class="label">SYNTHESIZED INSIGHT</span> <div class="content-text" id="${id}-summary">現在のページの構造からコア・メッセージを解析中...</div> </div> <div class="insight-box"> <span class="label">STRATEGIC STEPS</span> <ul class="action-list" id="${id}-steps"> <li>コンテキスト・マッチング待機中</li> <li>データの重要度重み付け中</li> </ul> </div> <button class="consolidate-trigger" id="${id}-run">情報を統合する</button> </div> `; document.body.appendChild(container); const toggleBtn = document.getElementById(`${id}-header`); toggleBtn.addEventListener('click', () => { container.classList.toggle('minimized'); document.getElementById(`${id}-toggle-icon`).innerText = container.classList.contains('minimized') ? '▶' : '◀'; }); const runBtn = document.getElementById(`${id}-run`); runBtn.addEventListener('click', () => { const headings = Array.from(document.querySelectorAll('h1, h2, h3')).map(el => el.innerText.trim()).filter(t => t.length > 0); const summaryEl = document.getElementById(`${id}-summary`); const stepsEl = document.getElementById(`${id}-steps`); summaryEl.innerText = '統合プロセス実行中...'; setTimeout(() => { if (headings.length > 0) { summaryEl.innerText = `「${headings[0]}」を中心軸とした${headings.length}個の主要トピックを、一貫性のあるナラティブに統合しました。`; stepsEl.innerHTML = ` <li>関連する${headings.length}セクションの優先順位付け完了</li> <li>現在の閲覧ペースに合わせた要約の再構成</li> <li>次の情報取得フェーズへのブリッジ構築</li> `; } else { summaryEl.innerText = 'ページ内の情報をスキャンし、最適化されたコンテキスト・ビューを生成しました。'; stepsEl.innerHTML = '<li>情報の断片化を抑制</li><li>認知的負荷の最小化</li>'; } }, 800); }); })();
