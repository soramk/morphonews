/**
 * MorphoNews Feature: エージェンティック・タービュレント・グラビティ・アンカー
 * Generated: 2026-02-28_0950
 * Description: 激動（Turbulent）する情報流の中を漂うニュース要素に対し、クリックで「重力場」を生成。散逸しようとする情報を物理的に引き寄せ、独自のコンテキスト・クラスタを形成して情報の氾濫を抑止します。
 */
(function() { const style = document.createElement('style'); style.textContent = ` .turbulence-field { position: relative; transition: all 0.5s cubic-bezier(0.2, 0.8, 0.2, 1); } .turbulence-active .news-card, .turbulence-active article, .turbulence-active .news-item { animation: morpho-float 10s infinite alternate ease-in-out; } @keyframes morpho-float { 0% { transform: translate(0, 0) rotate(0deg); } 33% { transform: translate(15px, -10px) rotate(1.5deg); } 66% { transform: translate(-10px, 15px) rotate(-1deg); } 100% { transform: translate(5px, 5px) rotate(0.5deg); } } #gravity-anchor-canvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 9998; } .gravity-well { position: absolute; width: 120px; height: 120px; border: 1px solid rgba(0, 255, 180, 0.4); border-radius: 50%; background: radial-gradient(circle, rgba(0, 255, 180, 0.15) 0%, transparent 75%); transform: translate(-50%, -50%); pointer-events: auto; cursor: crosshair; display: flex; align-items: center; justify-content: center; color: #00ffb4; font-family: monospace; font-size: 9px; box-shadow: 0 0 20px rgba(0, 255, 180, 0.2); animation: pulse-well 2s infinite; } @keyframes pulse-well { 0%, 100% { scale: 1; opacity: 0.8; } 50% { scale: 1.1; opacity: 0.4; } } `; document.head.appendChild(style); const canvas = document.createElement('div'); canvas.id = 'gravity-anchor-canvas'; document.body.appendChild(canvas); document.body.classList.add('turbulence-active'); const anchors = []; window.addEventListener('mousedown', (e) => { if (e.target.closest('.gravity-well')) return; const well = document.createElement('div'); well.className = 'gravity-well'; well.style.left = e.clientX + 'px'; well.style.top = e.clientY + 'px'; well.innerHTML = 'GRAVITY<br>ANCHOR'; canvas.appendChild(well); const anchorObj = { x: e.clientX, y: e.clientY, el: well }; anchors.push(anchorObj); well.addEventListener('dblclick', () => { anchors.splice(anchors.indexOf(anchorObj), 1); well.remove(); }); if (anchors.length > 4) { const oldest = anchors.shift(); oldest.el.remove(); } }); function updatePhysics() { const items = document.querySelectorAll('.news-card, article, .news-item'); items.forEach(item => { let pullX = 0; let pullY = 0; const rect = item.getBoundingClientRect(); const cx = rect.left + rect.width / 2; const cy = rect.top + rect.height / 2; anchors.forEach(a => { const dx = a.x - cx; const dy = a.y - cy; const dist = Math.sqrt(dx*dx + dy*dy); if (dist < 600) { const force = Math.pow((600 - dist) / 600, 2); pullX += dx * force * 0.2; pullY += dy * force * 0.2; } }); if (anchors.length > 0) { item.style.transform = `translate(${pullX}px, ${pullY}px)`; item.style.zIndex = '9997'; } else { item.style.transform = ''; } }); requestAnimationFrame(updatePhysics); } updatePhysics(); console.log('MorphoNews: Agentic Turbulent Gravity Anchor deployed. Click to create stability wells.'); })();
